{% extends 'pages/base.html.twig' %}

{% block title %}{{ 'home.tabs.team'|trans }} | {{ 'home.seo.title'|trans|raw }}{% endblock %}

{% block seo %}
    <meta name="description" content="{{ 'team.seo.description'|trans }}">
    <meta property="og:url" content="{{ app.request.uri }}">
    <meta property="og:type" content="website">
    <meta property="og:title" content="{{ 'team.seo.title'|trans }}">
    <meta property="og:description" content="{{ 'team.seo.description'|trans }}">
    <meta property="og:image" content="{{ absolute_url(asset('images/og.png')) }}">
{% endblock %}

{% block main_class %}bg-gray-50{% endblock %}

{% block content %}
    <twig:Team />
{% endblock %}
